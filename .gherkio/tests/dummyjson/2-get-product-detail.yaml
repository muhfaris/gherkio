scenario: Get product detail with matchers

steps:
  - request:
      method: GET
      url: /products/1

    expect:
      status: 200
      body.id: number
      body.title: string
      body.price: number
      body.rating: number
      body.tags: array
      body.dimensions: object

    timing:
      max: 5s
