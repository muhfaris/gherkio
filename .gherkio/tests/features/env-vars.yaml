scenario: environment variables integration

steps:
  # Using a dynamic product ID loaded from the host environment variables
  - request:
      method: GET
      url: /products/$GHERKIO_TEST_PRODUCT_ID

    expect:
      status: 200
      body.id: number
