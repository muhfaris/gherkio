scenario: Check Store Inventory (GET — Map Response)
steps:
    - request:
        method: GET
        url: /store/inventory
      expect:
        status: 200
      save:
        step1_inventory: body
      timing:
        max: 4s
