# RFC-10: Capability System

**Status:** Future (post-MVP)

A controlled plugin/extension system for adding capabilities to Gherkio without inline scripting in the DSL.

**Key decisions:**
- Not implemented in MVP
- Would provide controlled extensibility via plugins
- Maintains the constraint of "no arbitrary code execution in DSL"
