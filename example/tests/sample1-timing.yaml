scenario: login with timing

steps:
  - request:
      method: POST
      url: /auth/login
      body:
        username: emilys
        password: emilyspass

    expect:
      status: 200
      body.accessToken: exists

    save:
      token: body.accessToken

    timing:
      max: 1s
