scenario: login success

steps:
  - request:
      method: POST
      url: /auth/login

      body:
        email: admin@test.com
        password: secret

    expect:
      status: 200
      body.token: exists
      jwt.role: admin

    save:
      token: body.token
